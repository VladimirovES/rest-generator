
{% set docstring_indent = '    ' %}
from typing import Optional
from .rest_client.client import ApiClient

{% for srv in services %}
from .{{ srv.service_name }}.facade import {{ srv.api_class }}
{% endfor %}


class ApiFacade:
    def __init__(self, client: ApiClient):
        {% for srv in services %}
        self.{{ srv.service_name }} = {{ srv.api_class }}(client)
        {% endfor %}
