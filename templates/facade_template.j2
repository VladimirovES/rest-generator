{% set docstring_indent = '    ' %}
from typing import Optional

{% for imp in imports %}
from endpoints.{{ imp.module_name }} import {{ imp.class_name }}
{% endfor %}

class {{ facade_class_name }}:
    def __init__(self, auth_token: Optional[str] = None):
        """
        {{ facade_class_name }} — единый фасад для обращения к различным клиентам ({{ imports|length }}).

{{ docstring_indent }}:param auth_token: Токен аутентификации.
        """

        {% for imp in imports %}
        self.{{ imp.attribute_name }} = {{ imp.class_name }}(auth_token)
        {% endfor %}
